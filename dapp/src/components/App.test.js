import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

it('renders without crashing', () => {
  // TODO consider about redux
  const div = document.createElement('div');
  //ReactDOM.render(<App />, div);
});
